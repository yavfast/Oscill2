Prototype web app for Oscill2: FastAPI backend + vanilla JS frontend.

Features:
- Connect to device
- Acquire single frame
- Plot waveform on Canvas

Run the server:
- Using venv Python: ../.venv/bin/python -m uvicorn web_oscill.main:app --reload
